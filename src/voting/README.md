# Voting Module

This module implements the voting system used across competitions.

Components:
- `Vote`: Structs or models representing voting units.
- `Voting`: Logic for collecting votes, applying weights, and determining outcomes.

The voting subsystem is self-contained and can be reused or extended.
